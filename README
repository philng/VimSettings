NOTES
- jshint
- exuberant ctags
- vim 7.4+ with python

Need to copy vimrc from this repo to ~/.vimrc

# Setup Bundle
git submodule init
git submodule update

# Installing ctags and jstags
sudo apt-get install exuberant-ctags

# YouCompleteMe
cd ~/.vim/bundle/YouCompleteMe
./install.sh

# Tern
cd ~/.vim/bundle/tern_for_vim && npm install
install ctags
